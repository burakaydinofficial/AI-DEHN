# DEHN Development Environment Configuration
# Copy this file to .env.local and update the values with your actual credentials

# Environment
NODE_ENV=development
ENVIRONMENT=dev

# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=servusai-468510
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# Service URLs (for local development)
PDF_PROCESSOR_URL=http://localhost:3095
ADMIN_BACKEND_URL=http://localhost:3091
USER_BACKEND_URL=http://localhost:3090

# Database Configuration
# For local development, we'll use the deployed Firestore in development mode
FIRESTORE_DATABASE_ID=(default)

# Storage Configuration  
# For local development, we'll use the deployed bucket
GCS_BUCKET_NAME=dehn-documents-servusai-468510-dev

# Secrets (replace with actual values)
JWT_SECRET=dev-jwt-secret-replace-me-for-production
AI_API_KEY=your-google-gemini-api-key-here

# CORS Origins
ADMIN_CORS_ORIGINS=http://localhost:8091,http://localhost:5173
USER_CORS_ORIGINS=http://localhost:8090,http://localhost:5174

# Development Settings
DEBUG=true
PYTHONUNBUFFERED=1

# Port Configuration
PDF_PROCESSOR_PORT=3095
ADMIN_BACKEND_PORT=3091  
USER_BACKEND_PORT=3090
ADMIN_FRONTEND_PORT=8091
MOBILE_FRONTEND_PORT=8090
